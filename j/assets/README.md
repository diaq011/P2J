# UI Image Assets

Place your two reference images in this folder with the exact names below:

1. `title-j-simulator.png`  
   - Used by the home page top title image.

2. `bg-pixel-field.png`  
   - Used as the background image for all pages.

After copying files here, refresh the page (`Ctrl+F5`).
